
#ifndef TYPEDEF_H_
#define TYPEDEF_H_

#define DDPRINTF(...) {fflush(stdin); \
		fflush(stdout); \
		DPRINTF(__VA_ARGS__);\
		fflush(stdin); \
		fflush(stdout);}

typedef enum {
	Green_On,
	Green_Off,
	Yellow_On,
	Yellow_Off,
	Red_On,
	Red_Off
}Indicator_status;

#endif /* TYPEDEF_H_ */
